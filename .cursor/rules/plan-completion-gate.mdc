---
description: 原规划固定范围执行的收工门禁、验收真实性与主代理协调责任
alwaysApply: true
---

# 收工硬性门禁

- 原批准计划正文保持只读；复用已创建的待办，不重复创建或扩大范围。
- 主代理负责检查完成状态、整合子任务、串行验收和最终报告，不能仅依据子代理自述宣布成功。
- 任一必做待办未完成、任一已测失败未解决、任一必要外部验收未验证时，结论必须明确“还不能收工”，禁止宣称“全部通过”“全部完成”或同义结论。
- 外部验证仍仅三类：平台安装产物；真实系统交互；第三方登录/权限及真实AI/Python。隔离或模拟通过不能替代真实验收，也不能删除缺口以满足门禁。
- 全部通过须有当前最终源码对应的累计验收证据，核对实际测试数、退出码、参考与输入产物清单及提交；旧版本或多个定向结果不可相加替代。
- 条件不具备时如实报告阻塞和所需外部条件，继续可执行工作；不得捏造通过或无限重跑相同失败。
- 子任务可并行调查或编辑互不重叠的文件；构建、测试、源码/测试/脚本/package/out修改、提交的协调权仅在主代理。
- 任何构建测试开始前，所有写入者必须明确结束或确认冻结；运行期间冻结源码、测试、脚本、依赖清单、HEAD与out。
- 独立单元先基线，再最小修复、相关回归、commit和普通push origin/dev；不改main、不改写历史。
- 保留失败证据，修复测试夹具必须说明原因；不得放宽产品正确性断言、force点击或固定sleep掩盖问题。
- 不读真实用户配置/密钥，不修改用户Python环境，不关闭Electron后台bootstrap的网络、子进程、系统交互或webview保护。
- 每轮交付检查未完成、失败、外部未验证三类状态；规则本身不是自动测试或后台监控服务，不声称不存在的持续运行。
